rules:
  # release.yml is auto-generated by cargo-dist; modifications are overwritten on update
  # job-level permissions are set; workflow-level omission is intentional
  excessive-permissions:
    ignore:
      - release.yml
      - label.yml
      - rust-clippy.yml
      - semver-checks.yml
      - bench.yml
      - publish.yml
      - actions-lint.yml
      - codeql.yml
      - bsdtar-compat.yml
      - release-prep.yml
      - docs.yml
  undocumented-permissions:
    ignore:
      - release.yml
  # docs.yml needs persist-credentials for git push
  artipacked:
    ignore:
      - docs.yml
  template-injection:
    ignore:
      - release.yml
  # daily interval is intentional for this project
  dependabot-cooldown:
    disable: true
  # trusted publishing migration is tracked separately
  use-trusted-publishing:
    ignore:
      - publish.yml
  # secrets are correctly scoped in step-level env: blocks
  secrets-outside-env:
    ignore:
      - publish.yml
  # release.yml job definitions are auto-generated by cargo-dist
  anonymous-definition:
    ignore:
      - release.yml
  # peter-evans/create-pull-request auto-commits workspace changes and creates a PR;
  # this cannot be replaced by `gh pr create` alone
  superfluous-actions:
    ignore:
      - release-prep.yml
